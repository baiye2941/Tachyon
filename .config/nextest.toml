# nextest 配置
# 文档: https://nexte.st/docs/configuration/

[profile.default]
# 审计 E-04: 原 retries=2 用于消音 AppState::new() 测试的全局 .tachyon 竞态。
# 该测试已重构为纯函数测试(buffer_pool_capacity_for_config),零 IO/零 env 改动,
# 无需重试。全局重试会掩盖真实回归,故设为 0。
retries = 0
