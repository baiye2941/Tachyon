# 实现计划：P0-6 / P0-7 / P0-8（严格 TDD）

- 日期：2026-07-14
- Spec：`docs/aegis/specs/2026-07-14-p0-6-7-8-object-hls-bt-design.md`（已批准）
- 父 checkpoint：`docs/aegis/work/2026-07-14-phase0-correctness-fixes/20-checkpoint.md`
- 顺序：**P0-6 → P0-8 → P0-7**
- 提交：仅用户要求时 commit

```text
Plan Basis: approved design + audit P0-6/7/8
Requirement Ready Check: ready
Change Necessity: code-change
Existence Check: reuse-existing + ObjectIdentity 小类型
TDD Route: strict
ArchitectureReviewRequired: yes
```

## Files

| 切片 | 创建/修改 |
|---|---|
| P0-6 | `types.rs` ObjectIdentity；`traits.rs` range 签名；`test_harness`/`http`/`mirror`/`magnet`/`hls`/`downloader` mocks；`task_commands` inject；`downloader` plan 前比较 + 传 identity |
| P0-8 | `bt_storage.rs` open_sync + preferred；`magnet.rs` cache 绑定 + remove；`downloader` preferred 时机/cleanup |
| P0-7 | `downloader` URL→Hls；`hls` VOD 门；拒绝 mirrors+m3u8 |

## Compatibility

- Snapshot 字段不变
- Protocol range 增加 `identity: Option<ObjectIdentity>`（按值）
- 公开 API 行为收紧（resume/mirror 更安全）

## Tasks

### Task A1 — RED：ObjectIdentity 单元规则

1. 在 `crates/tachyon-core/src/types.rs`（或邻近 tests）写：
   - strong ETag 相等/不等
   - weak 不能单独证明兼容
   - Last-Modified fallback
   - size-only → resume 不安全 / mirror 不混拼 API（`is_safe_for_resume` / `compatible_for_mirror`）
2. 先写测试再写类型；期望编译失败或行为失败 → 再 GREEN 类型。

命令：`cargo nextest run -p tachyon-core -- object_identity`

### Task A2 — GREEN：ObjectIdentity + 导出

实现 `ObjectIdentity::{from_metadata, is_strong_etag, if_range_value, compatible_for_resume, compatible_for_mirror}`。

导出：`lib.rs` re-export。

### Task A3 — RED/GREEN：Protocol 签名 + 全实现者

1. 改 `traits::Protocol` range 方法签名。
2. 编译驱动修复所有 `impl Protocol`：
   - http / hls / magnet / mirror / test_harness / protocol lib mock / engine 内所有测试 mock / chaos_test
3. 暂不实现 If-Range 行为（占位 `_identity` 或透传）。

命令：`cargo check -p tachyon-engine -p tachyon-protocol -p tachyon-app --tests`

### Task A4 — RED：HTTP If-Range wiremock

在 `http.rs` tests：

- 服务端要求 `If-Range: "abc"` 才返回 206；无头返回 412/自定义失败
- client 在 identity 含 strong ETag 时必须带头
- weak ETag 不带 `If-Range`

期望 RED → GREEN 发头。

另：带 If-Range 收到 200 → 返回 Protocol 错误（禁止 200 fallback 截取）。

### Task A5 — RED/GREEN：engine 传 identity + resume 准入

1. `DownloadTask` 字段 `resume_object_identity: Option<ObjectIdentity>` + setter。
2. fragment path `download_range_stream(..., identity)` 使用 `metadata` 派生 identity。
3. `plan` 前：若 resume identity 与 probe metadata 不兼容 → 清空 completed/partial。
4. App `inject_resume_snapshot` 注入 snapshot etag/lm/size。
5. 测试：mock metadata 变更后 completed 不被使用（真实 plan/run 路径）。

### Task A6 — RED/GREEN：镜像身份筛选

1. `MirrorProtocol::probe` 后台成功源须 compatible。
2. 候选列表不补入不兼容源。
3. 测试：两源同长不同 ETag，最终内容不混。

### Task A7 — P0-6 交叉验证 + clippy/nextest

```bash
cargo nextest run -p tachyon-core -p tachyon-protocol -p tachyon-engine --locked
cargo clippy -p tachyon-core -p tachyon-protocol -p tachyon-engine --all-targets -- -D warnings
```

### Task B1 — RED/GREEN：多文件 open_sync

改 `bt_storage.rs` 多文件分支；加测试证明 create 路径不调用 block_on（或直接单测 open_sync 成功）。

### Task B2 — preferred 注入 factory

1. Factory 字段 `preferred_root_name`。
2. `DownloadTask` magnet 构造：preferred 在 probe 前写入 factory。
3. 测试路径 = preferred。

### Task B3 — cache binding + cancel remove

1. 扩展 HandleCache 值类型。
2. 命中校验 dir/factory/name。
3. cancel/complete/fail 清理 API + 测试。

### Task B4 — P0-8 验证

`cargo nextest run -p tachyon-engine -p tachyon-protocol -- magnet|bt_storage|handle`

### Task C1 — RED：DownloadTask m3u8 仍下 playlist

真实 run 测试期望失败（当前行为）→ 文档 RED。

### Task C2 — GREEN：URL 选择 HlsProtocol + VOD 门

构造期嗅探；live 错误；mirrors 拒绝。

### Task C3 — P0-7 验证 + 全切片收口

更新 `20-checkpoint.md` / `90-evidence.md`。

## Risks

- 全仓 Protocol mock 机械修改量大 → 先 `cargo check` 修编译
- librqbit remove API 差异 → 读锁定源码
- downloader 冲突域串行

## Retirement

- 删除 multi-file `Handle::block_on(open)`
- 删除镜像“全部源兜底混拼”
- 删除未绑定 cache 命中

## Execution

主审协调 strict TDD；可派只读 Tester/Reviewer；默认不 commit。
