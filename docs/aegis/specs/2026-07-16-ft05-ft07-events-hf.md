# Spec Brief：FT-05 事件失败可见 + 进度校准；FT-07 HF 专用 IPC

## 目标

1. **FT-05**：
   - `api/events.ts`：`listen` 注册失败 **reject**（不再吞成 no-op unlisten）
   - unlisten 仍安全
   - `useProgressListener`：注册失败 toast + 指数退避重连（上限）+ 周期 `refreshTaskList` 校准
2. **FT-07**：
   - `HfBrowserPanel` 单文件/批量下载改走 `batch_create_hf_tasks`（或现有 hub batch API），注入 `HfTaskMeta`
   - 不再 `get_hf_download_url` + 通用 `createTask` 作为主路径

## 非目标

- 完整离线指示 UI 文案系统
- 嗅探列表虚拟化（FT-06）
- FT-14 完整 grid/ARIA 重构（本轮可只补 icon button aria-label）

## TDD

- vitest events: mock listen reject → onProgressUpdate rejects
- useAppInit / useProgressListener 行为可 mock
- HF panel 调用 batch API（mock）
