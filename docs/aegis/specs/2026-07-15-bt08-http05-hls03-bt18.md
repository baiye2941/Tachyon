# Spec Brief：BT-08/09 · HTTP-05 · HLS-03 · BT-18

## 范围
1. **BT-08**：`peer_wait_timeout_secs=0`（peer_wait=MAX）时，无 peer 不得无限轮询；回退为 stall Timeout
2. **BT-09**：`ManagedTorrentPeerHealth::healthy` 与文档对齐，计入 `queued`
3. **HTTP-05**：限速 `acquire` 对必要等待不得截断为 5s 即放行；可分片 sleep，总等待覆盖 deficit
4. **HLS-03（最小）**：parse 阶段硬拒绝 `EXT-X-MAP` / `EXT-X-BYTERANGE`（防静默坏文件）
5. **BT-18**：`TachyonStorage::pwrite_all` 循环写满（short write 不提前成功）

## 非目标
- BT-17 piece 续传真相
- HTTP-13 redirect final-host 重占许可（需改 redirect 策略，另开）
- fMP4/MAP 完整实现

## TDD
strict
